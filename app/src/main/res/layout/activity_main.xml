<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 🗺️ PLAN + TRAJET (sera transparent et au-dessus de la caméra) -->
    <FrameLayout
        android:id="@+id/mapContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/mapImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/rdc_galilee"
            android:scaleType="centerInside"
            android:adjustViewBounds="true" />

        <com.example.sensortomatlab.PathOverlayView
            android:id="@+id/pathView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </FrameLayout>

    <!-- 📸 CAMÉRA (placée en arrière-plan, en bas de la stack) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center" />

    <!-- 🧭 PANEL D'INFO (toujours au premier plan) -->
    <LinearLayout
        android:id="@+id/infoPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center"
        android:background="#66000000"
        android:layout_gravity="top">

        <TextView
            android:id="@+id/textStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Streaming IMU + Camera vers MATLAB..."
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:layout_marginTop="20dp" />
    </LinearLayout>

</FrameLayout>
